/** @import {Queryable} from '@filecoin-station/deal-observer-db' */
/** @import {Provider} from 'ethers' */

/**
 * Observe the built-in actor events on the Filecoin blockchain to discover new FIL+ deals
 * @param {Queryable} pgPool
 * @param {Provider} rpcProvider
 */
export const observeBuiltinActorEvents = async (pgPool, rpcProvider) => {
  // TBD
}
